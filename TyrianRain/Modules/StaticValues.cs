using System;

namespace TyrianRain.Modules
{
    internal static class StaticValues
    {
        #region Guardian

        internal const float boltOfWrathDC = 1.0f;
        internal const float searingLightDC = 2.0f;
        internal const float seekingJudgmentDC = 3.5f;
        internal const float holyStrikeDC = 5.0f;
        internal const float holyStrikeHC = 0.2f;
        internal const float empowerHC = 0.5f;

        internal const float strikeDC = 1.5f;
        internal const float vengefulStrikeDC = 2.0f;
        internal const float wrathfulStrikeDC = 3.5f;
        internal const float whirlingWrathDC = 20.0f;
        internal const float whirlingWrathProjectileDC = 1.5f;
        internal const float leapOfFaithDC = 3.0f;
        internal const float leapOfFaithHC = 0.02f;
        internal const float bindingBladeDC = 1.0f;
        internal const float bindingBladeDotC = 30.0f;
        internal const float pullDC = 2.5f;

        internal const float aggressiveAegisDC = 3.0f;

        #endregion

        internal static string descriptionText = "Henry is a skilled fighter who makes use of a wide arsenal of weaponry to take down his foes.<color=#CCD3E0>" + Environment.NewLine + Environment.NewLine
             + "< ! > Sword is a good all-rounder while Boxing Gloves are better for laying a beatdown on more powerful foes." + Environment.NewLine + Environment.NewLine
             + "< ! > Pistol is a powerful anti air, with its low cooldown and high damage." + Environment.NewLine + Environment.NewLine
             + "< ! > Roll has a lingering armor buff that helps to use it aggressively." + Environment.NewLine + Environment.NewLine
             + "< ! > Bomb can be used to wipe crowds with ease." + Environment.NewLine + Environment.NewLine;

        internal const float swordDamageCoefficient = 2.8f;

        internal const float gunDamageCoefficient = 4.2f;

        internal const float bombDamageCoefficient = 16f;
    }
}